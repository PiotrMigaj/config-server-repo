spring:
  datasource:
    username: root
    password: Stefan0987
    url: jdbc:mysql://localhost/organisation_db?serverTimezone=Europe/Warsaw&createDatabaseIfNotExist=true
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      format_sql: true
      use_sql_comments: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  boot:
    message: 'Hello, Organisation Service Spring Cloud Bus Demo'

eureka:
  instance:
    client:
      serverUrl:
        defaultZone: http://localhost:8761/eureka/
